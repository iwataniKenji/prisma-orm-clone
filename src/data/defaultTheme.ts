import { createTheme } from "@mui/material";

export default createTheme({
  typography: {
    fontSize: 14,
    fontFamily: "'Inter', sans-serif",
  },
  palette: {
    primary: {
      main: "rgb(90, 103, 216)",
    },
  },
});
